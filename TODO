1. Implement a DP approach that uses the rightmost decomposition assumption by Lawler.
2. Construct an FPTAS that uses the scaling technique described in Koulamas paper [which page?] combined with the DP algorithm.

